import React from "react"

const Button = () => {
    return (
        <button className="bg-blue text-white font-semibold text-center w-full py-4.5 rounded-full text-base">
            Apply Now
        </button>
    )
}

export default Button
